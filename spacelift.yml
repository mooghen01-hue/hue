version: 1

stack:
  name: terraform-with-docker
  runner:
    labels:
      - docker

  terraform:
    version: "1.6.6"

workflows:
  default:
    plan:
      - run: ls
      - run: terraform init
      - run: terraform plan
    apply:
      - run: terraform apply -auto-approve
